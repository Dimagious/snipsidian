{
    "name": "snipsidian",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "esbuild src/main.ts --bundle --outfile=main.js --format=cjs --platform=node --target=es2020 --external:obsidian --external:electron --external:@codemirror/*",
        "build:watch": "npm run build -- --watch"
    },
    "devDependencies": {
        "esbuild": "^0.23.0",
        "obsidian": "^1.5.0",
        "typescript": "^5.5.0",
        "@types/node": "^20.12.12"
    }
}